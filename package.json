{
  "name": "@seneca/transport",
  "version": "0.0.1",
  "main": "dist/transport.js",
  "type": "commonjs",
  "types": "dist/transport.d.ts",
  "description": "Handle incoming messages within other frameworks.",
  "homepage": "https://github.com/senecajs/seneca-transport",
  "keywords": [
    "seneca",
    "transport"
  ],
  "author": "Richard Rodger - richardrodger.com",
  "repository": {
    "type": "git",
    "url": "git://github.com/senecajs/seneca-transport.git"
  },
  "scripts": {
    "test": "jest --coverage",
    "test-some": "jest -t",
    "test-watch": "jest --coverage --watchAll",
    "watch": "tsc -w -d",
    "build": "tsc -d",
    "clean": "rm -rf node_modules yarn.lock package-lock.json",
    "reset": "npm run clean && npm i && npm run build && npm test",
    "repo-tag": "REPO_VERSION=`node -e \"console.log(require('./package').version)\"` && echo TAG: v$REPO_VERSION && git commit -a -m v$REPO_VERSION && git push && git tag v$REPO_VERSION && git push --tags;",
    "repo-publish": "npm run clean && npm i && npm run repo-publish-quick",
    "repo-publish-quick": "npm run build && npm run test && npm run repo-tag && npm publish --access public --registry https://registry.npmjs.org "
  },
  "license": "MIT",
  "files": [
    "dist",
    "src/**/*.ts",
    "LICENSE"
  ],
  "engines": {
    "node": ">=14"
  },
  "dependencies": {
    "fast-safe-stringify": "^2.1.1",
    "qs": "^6.11.2"
  },
  "devDependencies": {
    "@seneca/maintain": "^0.1.0",
    "@types/jest": "^29.5.11",
    "@types/node": "^20.11.5",
    "@types/qs": "^6.9.11",
    "esbuild": "^0.19.11",
    "esbuild-jest": "^0.5.0",
    "jest": "^29.7.0",
    "seneca-msg-test": "^4.1.0",
    "typescript": "^5.3.3"
  },
  "peerDependencies": {
    "seneca": ">=3",
    "seneca-entity": ">=25",
    "seneca-promisify": ">=3"
  }
}
